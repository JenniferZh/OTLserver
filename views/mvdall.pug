extends layout

block content
    div(class = 'row')
        h1(class='my-3 col-md-8 offset-md-2') MVD
    each val in mvdlist
        div(class = 'row')
            div(class = 'alert alert-secondary p-3 col-md-8 offset-md-2' role=alert)
                h4 #{val.name}
                hr
                p NAMESPACE  #{val.ns}
                p URL        http://www.cbims.org.cn#{val.url}
                a(class='btn btn-primary float-right' href=val.url role='button') 详细信息